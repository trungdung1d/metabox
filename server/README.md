<!-- 
#1 npm install -g sequelize-cli  
#2 sequelize init
#3 sequelize model:generate --name namedatabase --attributes name:string, email:string,password:string
#4 sequelize db:migrate // khởi tạo bảng
--------------------Tạo dữ liệu mẫu bằng seeders-------------------------
#1 npx sequelize-cli seed:generate --name user
#2 npx sequelize-cli db:seed:all

-->